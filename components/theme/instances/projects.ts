import tealConfig from "../config/teal";

const projectsTheme: ThemeInstance = {
  config: tealConfig,
  animatedBackground: "projects",
  displayFavicon: "projects",
};

export default projectsTheme;
