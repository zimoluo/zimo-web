import cakeConfig from "../config/cake";

const birthdayTheme: ThemeInstance = {
  config: cakeConfig,
  displayFavicon: "birthday",
  animatedBackground: "birthday",
};

export default birthdayTheme;
