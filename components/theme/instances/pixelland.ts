import pixellandConfig from "../config/pixelland";

const pixellandTheme: ThemeInstance = {
  config: pixellandConfig,
  displayFavicon: "adaptive",
  animatedBackground: "pixelland",
};

export default pixellandTheme;
