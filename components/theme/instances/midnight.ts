import midnightConfig from "../config/midnight";

const midnightTheme: ThemeInstance = {
  config: midnightConfig,
  displayFavicon: "midnight",
  animatedBackground: "midnight",
};

export default midnightTheme;
