import scintillatingConfig from "../config/scintillating";

const scintillatingTheme: ThemeInstance = {
  config: scintillatingConfig,
  displayFavicon: "scintillating",
};

export default scintillatingTheme;
